process {
    withName:autodatabase_addtaxon {
        container = 'pyautodatabase:latest'
    }
    withName:autodatabase_mash {
        container = 'mash:latest'
    }
    withName:autodatabase_qc {
        container = 'pyautodatabase:latest'
    }
    withName:kraken2_databasebuild {
        container = 'kraken2:latest'
    }
}
docker {
    enabled = true
    runOptions = '-u $(id -u):$(id -g)'
}

